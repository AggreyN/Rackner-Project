# Copy this file to `.env` (in the frontend/ folder) and paste your real key.
# The real `.env` is gitignored — it will NEVER be committed. Keep it that way.
#
# Get a key at https://sam.gov  ->  Account Details  ->  API Key
SAM_API_KEY=your-key-here
