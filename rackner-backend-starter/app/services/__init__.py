"""Domain services: external data, ingestion, storage, secrets.

Each service has a dev stand-in selected by config, so the whole app runs with
no AWS account. See SCHEMA_v2.md and the Dev→prod switch table in the README.
"""
