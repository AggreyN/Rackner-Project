# Team Anvil backend — Week 1 (Foundation) dependencies
# Install with:  pip install -r requirements.txt
#
# Each week adds to this file. A checkout of the `week1` tag only needs these.

# --- Database ---
SQLAlchemy>=2.0
psycopg[binary]>=3.1      # PostgreSQL driver
alembic>=1.13             # database migrations (wired up in Week 3)
python-dotenv>=1.0        # loads your .env file
